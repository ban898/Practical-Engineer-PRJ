import React from "react";
import classes from "./TransparentButton.module.css";

const TransparentButton = () => {
  return <div className={classes.button}>Browse Pages</div>;
};

export default TransparentButton;
